<?php

/**
 * Our strings would normally be stored in the database, but for the sake of
 * creating a helper, we're defining them here.
 */

$string = array('HELLO_WORLD' => 'Hello, World');
$smarty->assign('STRING', $string);

?>
